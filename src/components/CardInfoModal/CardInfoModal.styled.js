import styled from "styled-components";

export const ModalInfoList = styled("ul")({
  display: "flex",
  flexWrap: "wrap",
  gap: 4,
  "&>li": {
    backgroundColor: "#0c8ce92b",
    padding: 8,
    borderRadius: 8,
  },
});
